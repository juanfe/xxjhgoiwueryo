The user exchange file will contain an object with the users as properties,
using as key the user ID, and as value the actual user with the properties
specified in the following structure:

{
    userId : { 
        userId : string,
        fundsAvailable : number
    }
}

* Notes on field values:
